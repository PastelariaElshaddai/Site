const $=id=>document.getElementById(id);let products=[];document.addEventListener("DOMContentLoaded",async()=>{let {data,error}=await supabaseClient.from("produtos").select("*").eq("ativo",true);products=data||[];render();$("search").oninput=render;if(error)$("products").textContent=error.message});function render(){let q=$("search").value.toLowerCase();$("products").innerHTML=products.filter(p=>(p.nome+" "+(p.descricao||"")).toLowerCase().includes(q)).map(p=>`<div class="product"><h3>${p.nome}</h3><p>${p.descricao||""}</p><b>R$ ${Number(p.preco).toFixed(2).replace(".",",")}</b></div>`).join("")||"Nenhum produto encontrado."}